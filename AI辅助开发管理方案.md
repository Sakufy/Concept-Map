# AI 辅助开发管理方案（可维护 · 可追踪 · 可持续迭代）

> 适用对象：个人开发者 + AI 编程助手（CodeBuddy / Claude Code / Cursor 等）
> 核心目标：让 AI 辅助的开发不再是"一次性对话"，而是**可持续的工程**
>
> **落地现状（2026-08-20 更新）**：本方案已在概念地图项目全量落地，实际执行的文档结构与第三节规划有两处差异，以实际为准：
> - **任务追踪**用 `iterations/v1-launch/.plan/plan.md`（阶段航图 + 关键决策）+ `phases/NN-*.md`（阶段详情），**不另建** `docs/TASKS.md` 与 `docs/ADR/`（避免双轨与重复记录）；
> - 文档统一索引与更新规则见 **`docs/README.md`（文档地图）**。

---

## 一、先理解问题：AI 开发为什么会失控

基于对长周期 AI 开发项目的调研（Coding Harness 工程化实践、Claude Code 上下文治理、Spec Kit 官方文档），AI 辅助开发有三大公认顽疾：

| 顽疾 | 表现 | 根因 |
|------|------|------|
| **上下文遗忘** | 隔几天继续开发，AI 忘了架构约定、风格规范 | 会话不持久，全靠记忆文件支撑 |
| **错误累积** | 改一处崩三处，越改越乱 | 没有测试兜底，AI 改代码无法自查 |
| **过早宣告完成** | AI 说"做完了"，实际功能残缺、验收不过 | 没有规格对照物和验收清单 |

**结论：AI 开发需要一套"护栏"——用文档约束行为、用清单验收结果、用记录沉淀历史。** 这就是本方案存在的意义。

---

## 二、方案总览：三个支柱

```
┌────────────────────────────────────────────────────────┐
│  支柱一：规格驱动开发（Spec-Driven Development）         │
│  先写规格，再写代码 —— AI 有据可依，进度有对照物        │
├────────────────────────────────────────────────────────┤
│  支柱二：三层文档体系（对抗上下文遗忘）                  │
│  项目层(不变量) → 迭代层(当前) → 记录层(历史)           │
├────────────────────────────────────────────────────────┤
│  支柱三：质量门禁（对抗错误累积与过早完成）              │
│  测试 + DoD 验收清单 + 人工复核，缺一不可               │
└────────────────────────────────────────────────────────┘
```

### 支柱一：规格驱动开发（SDD）

借鉴 **GitHub 官方开源的 Spec Kit**（github.com/github/spec-kit，2025 年发布，专为 AI 编程时代设计的规格驱动开发工具包）。核心思路：

> AI 编程代理默认走最短路径——跳过规格、跳过测试、跳过所有让软件可靠的东西。SDD 强制"先想清楚，再动手"。

**五阶段循环（每个功能/迭代都走一遍）：**

```
Specify（规格化）→ Plan（计划）→ Implement（实现）→ Test（验证）→ Iterate（迭代）
   │                 │               │              │              │
 写清楚做什么        拆成小任务       按计划写代码    跑测试+验收    复盘并进入下一轮
 为什么、验收标准    排优先级          并用测试验证    不通过就返工    持续收敛
```

- 规格（Spec）是**给 AI 看的合同**：功能描述 + 验收标准 + 边界条件
- 任务（Task）是**进度追踪的最小单元**：可独立验证、有明确完成定义
- 测试是**AI 的自检手段**：让 AI 改完代码后自己验证"没改坏别的东西"

> 配套工具：Spec Kit 提供 `specify` CLI 与 VS Code 扩展，支持任务 checkpoint/resume（中断可恢复）。个人项目即使不装工具，也可以用手动文件夹 + 清单的方式执行同一套流程（见下文文件结构）。

### 支柱二：三层文档体系

文档是 AI 的"长期记忆"。按更新频率分为三层，每层职责清晰：

| 层级 | 文件 | 更新频率 | 作用 |
|------|------|---------|------|
| **L1 项目层**（每次会话自动加载） | `AGENTS.md`、`README.md`、开发方案 | 几乎不变 | 告诉 AI 这个项目是什么、技术栈、编码规范、约束边界 |
| **L2 迭代层**（每次开工先读） | `docs/ROADMAP.md`、`docs/TASKS-当前迭代.md`、`docs/ADR/` | 每次迭代更新 | 当前在哪、这轮做什么、做过哪些关键决策 |
| **L3 记录层**（沉淀历史） | `docs/CHANGELOG.md`、工作记忆日志、Git 提交历史 | 持续追加 | 出了什么、为何这样改、可回溯 |

**关键机制：**
- **AGENTS.md**：给 AI 看的 README（开放格式，Anthropic/OpenAI 官方推荐）。放在项目根目录，包含构建命令、编码规范、测试要求、技术栈版本。AI 每次开工自动读取，大幅减少"上下文幻觉"。
- **ADR（架构决策记录）**：每当做一个不可轻易回头的决定（选型、数据结构、架构），记录"在什么约束下，为什么选 B 而非 A"。防止 AI 以后"推翻重来"或重复犯错。
- **CHANGELOG + Conventional Commits**：提交信息遵循 `feat:`/`fix:`/`refactor:` 等前缀，AI 与人类都能快速看懂变更历史，将来可自动生成发布日志。

### 支柱三：质量门禁（Definition of Done）

每个任务"算完成"必须全部满足（写进 AGENTS.md 让 AI 严格执行）：

```
□ 功能按规格实现（对照 Spec 逐条核对）
□ 已跑通构建（npm run build / tsc 无错误）
□ 已跑通测试（vitest 等，新增代码有测试）
□ 无 lint 错误
□ 关键交互经人工点击验证（AI 无法代替人判断体验）
□ 已更新 CHANGELOG 与记忆日志
□ 已提交 Git（Conventional Commits 格式）
```

---

## 三、落地文件结构（概念地图项目专用）

```
概念地图项目/
├── AGENTS.md                    ← L1 给 AI 看的项目规范（核心！）
├── README.md                    ← 人类看的项目说明（含文档导航）
├── 概念地图软件开发方案.md        ← 技术选型与路线图（已有）
├── AI辅助开发管理方案.md          ← 本文档
├── docs/
│   ├── README.md               ← 【文档地图】全部文档的定位与更新规则（入口）
│   ├── CHANGELOG.md             ← L3 变更日志（按日期追加）
│   └── Lynkage实现分析.md       ← L1 竞品逆向分析（技术依据）
├── iterations/
│   └── v1-launch/              ← L2 当前迭代
│       ├── PRD.md               ← 产品需求（做什么 / 不做什么）
│       └── .plan/
│           ├── plan.md          ← 全项目阶段航图 + 关键决策（任务追踪单一来源）
│           └── phases/NN-*.md   ← 阶段详情（验收判据 / sections / 自测记录）
├── src/                         ← 实际代码（Phase 1 起）
│   └── __tests__/               ← 测试与代码同层存放
├── .codebuddy/
│   └── memory/                  ← 工作记忆（AI 跨会话上下文，已启用）
└── .gitignore
```

> 注：ROADMAP / TASKS / ADR 三件套由 `plan.md`（航图）+ `phases/`（任务）+「关键决策」节（ADR 作用）替代，避免文档双轨。结构图与更新规则详见 `docs/README.md`。
---

## 四、标准工作流：一个迭代怎么跑（以概念地图项目为例）

**迭代周期建议：1 周 = 1 个可演示的小里程碑。** 参考路线图拆解：

### 每周循环（Repeat）

```
周一【计划】   从 ROADMAP 选下一里程碑 → 拆成 3~6 个可验证任务 → 写进 TASKS.md
周二~四【实现】 每个任务按 SDD 循环执行（见下）
周四【验证】   跑全部测试 + lint + build → 你亲自点一遍验收 → 勾选 DoD
周五【复盘】   更新 ROADMAP 状态、写 CHANGELOG、归档 ADR、写工作记忆
```

### 单个任务循环（SDD 五步）

以"实现概念节点拖拽"为例：

| 步骤 | 做什么 | 产物 |
|------|--------|------|
| **1. Specify** | 用一段话写清楚：功能、边界、验收标准 | TASKS.md 中的任务描述 |
| **2. Plan** | 拆解为：引入 React Flow → 配置节点组件 → 处理拖拽事件 → 写测试 | 任务下的子步骤 |
| **3. Implement** | 让 AI 按计划实现，要求"实现完自己跑测试" | 代码 + 测试 |
| **4. Test** | 跑 `vitest` + `npm run build`；你人工拖拽验证一次 | 测试通过记录 |
| **5. Iterate** | 通过 → 勾选完成；不通过 → 回到 2/3，禁止带病进入下个任务 | 状态更新 |

---

## 五、进度追踪机制（"一切进度可追踪"）

1. **任务统一编号**：每个任务有 ID（如 `T-001 搭建Vite工程`），全项目唯一。
2. **三态状态机**：`待办 → 进行中 → 已完成(含验证人)`。TASKS.md 里用 checkbox 呈现，一眼看到进度。
3. **里程碑对照**：ROADMAP.md 每行一个里程碑，标注 `规划/进行中/完成`。
4. **变更可回溯**：Git 提交 + CHANGELOG + ADR 三处记录，任何时候能回答"为什么这样"。
5. **跨会话连续性**：每次结束工作，AI 自动把当日进展写进 `.codebuddy/memory/`；下次开工 AI 自动读取，无缝续接。
6. **定期健康检查**（可选）：设置 CodeBuddy 自动化任务，每周自动回顾 ROADMAP、检查是否有任务"带病完成"。

---

## 六、工具链建议（轻量起步，按需添加）

| 用途 | 工具 | 阶段 |
|------|------|------|
| 版本控制 | Git + GitHub 远程仓库（备份 + 可溯源） | Phase 0 必做 |
| 提交规范 | Conventional Commits（`feat:`/`fix:`/`refactor:`/`docs:`/`test:`） | Phase 0 |
| 测试 | Vitest（前端主流，与 Vite 同生态） | Phase 1 |
| 构建校验 | `npm run build` + ESLint + TypeScript | Phase 1 |
| 规格工作流（可选） | GitHub Spec Kit（`specify` CLI / VS Code 扩展） | 熟悉后按需 |
| CI（可选） | GitHub Actions：push 自动跑 lint + test + build | Phase 2+ |
| 文档自动生成（可选） | CHANGELOG 由提交记录自动生成 | Phase 2+ |

---

## 七、在 CodeBuddy 中的具体落地动作

1. **建立 `AGENTS.md`**（已随本方案创建模板，见工作区根目录）——AI 每次会话自动遵守。
2. **维护 `.codebuddy/memory/`** 工作记忆——AI 自动读写，跨会话记住进展与决策。
3. **按 ROADMAP 拆迭代**，把 TASKS.md 作为每天对话的开场白："读取 TASKS.md，继续 T-003"。
4. **每个任务收尾时强制验收**：让 AI 汇报"测试结果 + DoD 勾选情况"，你只做最后点击验证。
5. **重要决策写 ADR**：告诉 AI"先读 ADR 再动手"，避免推翻已有决策。

---

## 八、启动清单（落地状态 2026-08-20）

- [x] 已完成：《概念地图软件开发方案.md》（技术选型 + 路线图）
- [x] 已完成：《AI辅助开发管理方案.md》（本文档）
- [x] 已创建 `AGENTS.md`（项目规范，每次会话自动加载）
- [x] 已创建 `README.md`（人类入口 + 文档导航）
- [x] 已创建 `docs/CHANGELOG.md`（变更日志，持续追加中）
- [x] 已创建 `docs/README.md`（文档地图，统一索引与更新规则）
- [x] ROADMAP / TASKS / ADR → 用 `iterations/v1-launch/.plan/plan.md`（航图 + 关键决策）+ `phases/NN-*.md`（任务详情）替代
- [ ] 初始化 Git 仓库 + GitHub 远程（本地已有 dev 工作流，提交规范待启用）
- [x] Phase 1~3 已全部完成（画布/概念/连线/持久化/云同步/嵌入式/贝塞尔），当前在 v3 收尾体验优化专项
