# 概念地图软件（对标 Lynkage）开发方案

> 目标：开发一款可在**电脑 + 手机**上使用的概念地图软件，支持自由画布、概念节点、带连词的连接线、内嵌节点、资源挂载、多设备同步与分享。
>
> 依据：Lynkage 实现方法逆向分析（见 `docs/Lynkage实现分析.md`，2026-08-20 完成）。方案已按逆向结论更新连词建模与功能范围。
>
> **演进说明（2026-08-20，正文保留当时决策不作改动）**：
> - 第三节"连词渲染降级为 edge label"已**不再适用**——用户拍板 v1 即做**连词独立节点**（对齐 Lynkage 最终形态）：一条命题 = 概念→连词 + 连词→概念 两条 Connection + 一个 LinkingPhrase 节点；直连（Ctrl+Shift）= 单条 Connection（`viaId=null`）。数据层三元组 Schema 未变，变化的是渲染层升级。
> - 实际开发进度以 `iterations/v1-launch/.plan/plan.md`（全项目航图）为准：Phase 1~3 已完成（画布/概念/连线/持久化/云同步/嵌入式/贝塞尔），当前在 v3 收尾体验优化专项。
> - 文档结构与更新规则见 `docs/README.md`（文档地图）。

---

## 一、Lynkage 核心功能拆解（对标清单）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 无限自由画布 | 无边界、无中心，可平移缩放 | P0 |
| 概念节点 | 自由摆放、拖拽、双击编辑文字 | P0 |
| 连词（Linking Phrase） | 表达"概念—连词—概念"三元关系 | P0 |
| 嵌入式节点 | 节点内嵌子节点，表达包含关系 | P1 |
| 资源挂载 | 节点/连线上附加文字、链接、图片、文件 | P1 |
| 多图管理 | 多个地图的创建、列表、归档 | P1 |
| 云同步 + 账号 | 手机与电脑数据互通 | P1 |
| 分享 | 只读/可编辑链接分享（含 iframe 嵌入） | P2 |
| 导入导出 | JSON / PNG / Markdown 导出 | P2 |
| 元素样式面板 | 节点/连线/连词分别调样式（颜色/边框/曲线/字号） | P1 |
| 背景与主题 | 纯色 / 网格 / 图片背景；全局主题 | P2 |
| 概念图统计 | 节点数 / 连词数 / 命题数统计 | P2 |
| 历史版本 | 时间线回溯 | P2 |
| 演示模式（Frames） | 按帧逐页演示概念图 | P2 |
| 协作制图 | 多人在线编辑（Realtime） | P3 |

---

## 二、技术选型（推荐方案）

### 总体架构：Web 应用（PWA）——一套代码双端通用

```
┌─────────────────────────────────────────────┐
│  前端：React + TypeScript + Vite + Zustand  │
│  画布：@xyflow/react (React Flow)           │
│  本地缓存：IndexedDB（离线优先）             │
├─────────────────────────────────────────────┤
│  云端：Supabase（Postgres + 认证 + Realtime）│
│  部署：CloudBase / EdgeOne Pages / Vercel   │
└─────────────────────────────────────────────┘
```

**为什么选 Web/PWA 而非原生 App？**

| 方案 | 电脑 | 手机 | 成本 | 说明 |
|------|------|------|------|------|
| Web (PWA) ✅ | 浏览器即用 | 浏览器即用，可"添加到主屏幕" | 一套代码 | 无需上架商店，个人开发最快 |
| Capacitor 包装 | 桌面可选 | App Store/Google Play | 在 PWA 基础上加壳 | 有上架需求时再升级 |
| Flutter / React Native | 需分别适配 | 原生体验 | 两套 UI 逻辑 | 概念图重交互，前端 Web 生态更成熟 |
| 桌面原生 | 体验好 | ✗ | 高 | 放弃移动端，不推荐 |

### 画布引擎选型对比

| 引擎 | 拖拽/连线/缩放 | 自定义节点 | 连词标签 | 社区 | 结论 |
|------|--------------|-----------|---------|------|------|
| **@xyflow/react (React Flow)** ✅ | 全内置 | 强 | 易（自定义 edge label） | 大 | **首选**：交互完备、开发快 |
| AntV G6 | 强（侧重图布局） | 中 | 中 | 中 | 做自动布局/大数据图时用 |
| Konva.js | 自研 | 自研 | 自研 | 大 | 最灵活但工作量大 |
| 自研 D3+SVG（Lynkage 做法） | 自研 | 自研 | 自研 | — | Lynkage 用此方案，但开发量大 3~5 倍 |

> 结论：**React Flow 起步**。逆向分析显示 Lynkage 的画布本质是"SVG 连线层 + HTML 节点覆盖层"，React Flow 正是同一模式的现成实现，可覆盖 Lynkage 90% 画布能力。后续若需自动布局（力导向/层级），再引入 `d3-force` 或 `elkjs`。

### 后端与数据

**首选 Supabase**（本项目规则已含 Supabase 接入说明，可一键对接）：
- Postgres 数据库：存画布数据
- Auth：邮箱/手机登录，多设备同步的前提
- Realtime：实时协作基础
- 免费额度足以支撑个人/教学使用

> 后端启示（来自 Lynkage 逆向）：Lynkage 后端用 **Neo4j 图数据库**存概念图三元组数据。MVP 不需要图数据库，但**数据模型从第一天就按三元组设计**，未来若要升级图查询（路径/相似度/自动推荐关联概念），可平滑迁移到 Neo4j / Postgres+Apache AGE。

---

## 三、数据模型设计（关键更新）

### 设计原则：三层数据模型，两段式渲染

Lynkage 的核心架构发现：**连词是独立实体，不是边的标签**。数据模型固定为"概念 / 连词 / 连接"三元结构，永不改；渲染层 MVP 阶段把连词"降级"渲染为 edge label，后续可升级为独立节点而无需迁移数据。

```
数据层（稳定）          渲染层（可演进）
──────────────────────────────────────────
CONCEPT ──────┐         Phase 1-2:  React Flow Node
              ├──────── 概念节点
LINKING_PHRASE│         Phase 1-2:  渲染为 edge label 文本
              │         Phase 4:    升级为独立节点（拖拽、挂附件）
CONNECTION ───┘         React Flow Edge
```

### JSON Schema（本地 IndexedDB 与云端共用）

```json
{
  "schemaVersion": 1,
  "id": "map-xxx",
  "title": "光合作用",
  "theme": "default",
  "background": { "type": "grid", "color": "#ffffff" },
  "concepts": [
    {
      "id": "c1",
      "type": "concept",
      "text": "光合作用",
      "x": 100, "y": 200, "w": 160, "h": 60,
      "style": { "fill": "#e3f2fd", "borderColor": "#1976d2", "fontSize": 16 },
      "parentId": null
    }
  ],
  "linkingPhrases": [
    { "id": "lp1", "type": "linkingPhrase", "text": "需要" }
  ],
  "connections": [
    {
      "id": "conn1",
      "type": "connection",
      "fromId": "c1",
      "toId": "c2",
      "viaId": "lp1",
      "controlPoints": [],
      "style": { "curve": "straight" }
    }
  ],
  "frames": [],
  "createdAt": "...",
  "updatedAt": "..."
}
```

字段说明：
- `viaId` 可空（`Ctrl+Shift` 直连时无连词）
- `controlPoints` 为贝塞尔曲线控制点（Lynkage 的"贝塞尔 4 控制点曲线"）
- `frames` 为演示帧（P2 功能，数据层预留）
- `schemaVersion` 用于数据迁移（Lynkage 的 `formatVersion` 同款思路）

### SQL 存储（Supabase，MVP 简化版）

```sql
create table maps (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid references auth.users,
  title text default '未命名概念图',
  data jsonb not null default '{}',          -- 上述完整 JSON（含 schemaVersion）
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);
```

> MVP 阶段整图存一张 JSONB（简单可靠），多设备同步用 `updated_at` + 版本号。协作/局部更新需求出现后再拆 `concepts` / `linking_phrases` / `connections` 独立表。

---

## 四、交互设计（借鉴 Lynkage 已验证的交互）

### 核心交互清单

| 交互 | 实现方式 | 优先级 |
|------|---------|--------|
| 创建概念 | **双击画布**，默认占位文本"???"并立即进入编辑 | P0 |
| 创建命题 | 节点上的 **4 向拓展箭头**拖出连线，**自动补全连词**（占位文本，用户再改） | P0 |
| 直连 | `Ctrl/Cmd + Shift` 拖拽跳过连词 | P0 |
| 编辑文本 | 双击节点内联编辑；右键弹窗富文本编辑（长文本） | P0 |
| 曲线 | 选中连线 → 样式面板 → 贝塞尔曲线 → 拖拽控制点 | P1 |
| 嵌入式节点 | 右键"添加嵌入子节点"；`Alt + 拖拽` 移入/移出父节点 | P1 |
| 移动画布 | `Space + 左键拖拽`；右下角 **minimap** 导航视窗 | P1 |
| 粘贴 | `Ctrl/Cmd + V` 粘贴文本 / 图片 / 概念 / 连词 | P1 |
| 删除 | 选中后 Delete / Backspace | P0 |
| 撤销/重做 | `Ctrl+Z` / `Ctrl+Shift+Z` | P0 |

### 自定义节点设计（对齐 Lynkage 体验）

Lynkage 节点 = HTML div + Slate 富文本，支持多行、Markdown、LaTeX（MathJax）。React Flow 中自定义节点实现：
- 节点本体：HTML div（圆角矩形、边框、颜色由 CSS/内联样式控制）
- 文本：`contentEditable` div（MVP）；如需 LaTeX 再引入 MathJax/katex
- 连线手柄：React Flow `Handle` + 自定义"拓展箭头"（4 向小箭头按钮，拖出时创建新连词）
- 嵌入式：子节点绝对定位在父节点内，`parentId` 字段 + React Flow 的 parentNode 能力

---

## 五、开发路线图

### Phase 1 — MVP 画布骨架（约 1~2 周）
- [ ] Vite + React + TS 脚手架，引入 React Flow + Zustand
- [ ] 无限画布：平移、缩放、框选、minimap
- [ ] 添加/拖拽/删除概念节点，**双击画布新建（默认"???"立即编辑）**
- [ ] 节点间拖线连接（带 label，即连词），连线删除
- [ ] 数据按三元组 Schema 存 IndexedDB + 导出/导入 JSON
- [ ] 移动端触摸手势验证（单指平移、双指缩放、点击选中）

### Phase 2 — 核心概念图能力（约 2~3 周）
- [ ] **拓展箭头拖线 + 自动补全连词**（自定义节点 Handle）
- [ ] `Ctrl+Shift` 直连；右键弹窗富文本编辑
- [ ] 连词/连线样式面板（颜色、边框、曲线类型切换）
- [ ] 贝塞尔曲线控制点拖拽
- [ ] 嵌入式节点（parentId + Alt 拖入拖出）
- [ ] 撤销/重做（历史栈）；快捷键体系

### Phase 3 — 云端与多端（约 2~3 周）
- [ ] 接入 Supabase：注册/登录、地图列表页（新建/重命名/删除/归档）
- [ ] 云端保存与加载（登录态自动同步、乐观更新 + 版本比对）
- [ ] PWA 化：manifest + Service Worker 离线可用、"添加到主屏幕"
- [ ] 多图管理（对应 Lynkage `mapModels[]`）

### Phase 4 — 增强功能（按需）
- [ ] 连词升级为独立节点（数据不变，渲染升级：连词可拖拽、挂附件）
- [ ] 资源挂载：节点/连词附件（文本、链接、图片、文件 → Supabase Storage）
- [ ] 分享链接（只读/可编辑，支持 iframe 嵌入参数）
- [ ] 导出 PNG / 思维导图 Markdown
- [ ] 概念图统计面板（节点/连词/命题数）
- [ ] 背景与主题切换
- [ ] Frames 演示模式（数据层已预留 `frames` 字段）
- [ ] 历史版本回溯（版本表）
- [ ] 自动布局（力导向 / 环形 / 分层）
- [ ] 实时协作（Supabase Realtime 广播）
- [ ] （可选）Capacitor 打包上架移动应用商店

---

## 六、推荐的开发顺序与最小投入

1. **先跑通"自由画布 + 节点 + 连线 + 连词标签 + 本地保存"** —— 1~2 周出一个可玩的版本
2. **再加拓展箭头拖线、样式面板、内嵌节点** —— 这是 Lynkage 的核心差异化
3. **再接云同步** —— 电脑手机互通的关键
4. **最后做分享/统计/演示/协作** —— 锦上添花

技术栈精简版（最小依赖）：
```
react + react-dom + @xyflow/react + zustand + idb-keyval
supabase-js（Phase 3 引入）
vite + @vitejs/plugin-react（构建）
```

---

## 七、风险与注意点

| 风险 | 应对 |
|------|------|
| 连词建模两段式带来的复杂度 | 数据层锁死三元组 Schema；渲染降级/升级互不影响（`schemaVersion` 兜底迁移） |
| 移动端触摸与桌面鼠标交互差异 | React Flow 已统一处理；重点测试双指缩放与拖拽冲突 |
| 自定义节点（拓展箭头/富文本）开发量 | 先做最小版（contentEditable + 单向箭头），Slate/MathJax 后置 |
| 大数据量画布卡顿 | 节点超 500+ 时考虑虚拟化（React Flow 自带 viewport 裁剪优化） |
| 多设备同步冲突 | 单用户场景用"最后写入优先 + 版本号"足够；协作再升级到 CRDT |
| 概念图数据迁移/兼容 | 数据文件统一带 `schemaVersion`，迁移逻辑按版本递增写 |

---

## 八、下一步

我可以直接在本工作区为你搭建 Phase 1 的 MVP 工程（React + React Flow + 三元组数据模型 + 本地保存 + 移动端适配），跑通后在浏览器里直接预览，再逐步接入 Supabase 云同步。
